<%= simple_form_for  @circularNb, url: circular_prime_index_path do |f| %>
  <div class="container-form">
      <%= f.input :value, label: "",  placeholder: "Enter a value", input_html: {class: "input-form"} %>
      <div class="text-center space-top"> <%= f.submit "Validate", class: 'btn btn-primary' %> </div>
  </div>
<% end %>
